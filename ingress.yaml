apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hw-ingress
  namespace: homework
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /index.html
spec:
  rules:
  - host: homework.otus
    http:
      paths:
      - path: /homepage
        pathType: Prefix
        backend:
          service:
            name: hw-service
            port:
              number: 80
      - path: /index.html
        pathType: Prefix
        backend:
          service:
            name: hw-service
            port:
              number: 80        
